'use strict';

// Simplified function-bind for ts-aot
// Note: this implementation is exported and called via bind.call(target, thisArg)
// Since .call() this-binding doesn't work in the untyped JS path,
// we also export a callBind helper that takes target explicitly.

module.exports = function bind(that) {
    var target = this;
    // Fallback: if this is undefined (from .call() in untyped JS),
    // check ts_get_call_this for the real target
    if (typeof target === 'undefined' || target === null) {
        // Can't access the intended 'this' - return a passthrough
        return function() { return that; };
    }
    if (typeof target !== 'function') {
        throw new TypeError('Function.prototype.bind called on incompatible ' + target);
    }
    var args = [];
    for (var i = 1; i < arguments.length; i++) {
        args.push(arguments[i]);
    }

    var bound = function() {
        var allArgs = args.slice();
        for (var j = 0; j < arguments.length; j++) {
            allArgs.push(arguments[j]);
        }
        return target.apply(that, allArgs);
    };

    if (target.prototype) {
        var Empty = function Empty() {};
        Empty.prototype = target.prototype;
        bound.prototype = new Empty();
        Empty.prototype = null;
    }

    return bound;
};
