'use strict';

var $reflectApply = require('./reflectApply');

// Simplified: use Reflect.apply if available, otherwise a simple wrapper
module.exports = $reflectApply || function actualApply(func, thisArg, args) {
    return func.apply(thisArg, args);
};
