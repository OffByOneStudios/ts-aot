# SMELL-002: Code-smell pass — compiler + runtime (2026-07-12)

## STATUS: ALL THREE TIERS COMPLETE (2026-07-12)

### Tier 3 (2026-07-12, second half of session)
- Item 9 (33b70078 + TsIterator.cpp extraction): TWO LIVE BUGS fixed —
  (a) ts_iterator_get's string fallback ToString-coerced numbers, so
  `for (v of 12)` iterated the string "12" instead of throwing (gated the
  fallback on pointer-shaped nanbox values); (b) Iterator.concat's result
  had no prototype — toArray/map/filter unreachable (now inherits
  %IteratorPrototype%). Family gates: for-of +5, dstr +4, Iterator +1,
  0 lost. STALE survey items: yield* result-object guard already present
  via ts_iterator_next; GetIteratorDirect "duplication" is two 3-line
  caches (non-smell). ts_iterator_* family moved to new TsIterator.cpp
  (was in TsPromise.cpp); create_generator_result stays, declared cross-TU.
  Concat's legacy array-shaped inner walk kept DELIBERATELY: several
  runtime @@iterator impls still return plain arrays.
- Item 12 (TsTemporal POD-ify): 93 anchor-patch ops across 32 functions —
  every std::string local now constructed at function top before the first
  throwing call. Verified with a 5,200-iteration throwing-path battery
  (tmp/ptemp12.js); Temporal family 3,992/0 lost.
- Item 11 (src/shared/BuiltinGlobals.inc): one X-macro list now generates
  lowerLoadGlobal's known-name map (was the 80-branch REFACTOR-NEEDED
  chain), ts_global_ctor_by_name, and the ASTToHIR builtinObjects set
  (second, strictly-subsumed dead copy removed). Membership encoded
  exactly as-was; the three-table drift class is now impossible. -175 lines.
- Item 10 (scoped by measurement): entry-point unification (ToPropertyKey
  once + symbol canonicalization at ts_object_get_dynamic_checked) was
  ALREADY in place; the survey's "symbol-keyed accessors on TsFunction
  missed" is STALE (probe-clean, fixed by the recent accessor work).
  Probing per-type leaf handlers found THREE REAL divergence bugs, fixed:
  Set instances ignored defineProperty accessors AND own data writes
  (`s.plain = 7` read back undefined); RegExp instances ignored
  defineProperty accessors. Both leaf handlers now do the OrdinaryGet
  own-props consultation (accessor first, then data) like the Date/Promise
  arms. Probes tmp/pget10.js + tmp/pget10b.js (20 ladder-consistency
  checks, all green). Residual: ~12 single-level own-slot __getter_ checks
  in descriptor/defineProperty code are correct as-is; full "single
  accessor walk" consolidation remains available but has no measured
  defect behind it — do it only with a dedicated sweep budget.

## TIER 1 + TIER 2 (2026-07-12, 8 commits 1658c8e8..19406eb7)
- Item 1+2 (1658c8e8): ts_value_is_object canonical + single typeof engine.
  Iterator.concat BIGI exclusion attempted+REVERTED (Object(1n) IS the raw
  TsBigInt — no wrapper objects; family gate caught -1).
- Item 3 (7ec37901): table gaps (Iterator→lowerLoadGlobal; Proxy+GenFn
  family→ctor-by-name; Intl/BigInt/TypedArrays→builtinObjects).
- Item 4 (ba3844ce): HIGH longjmp frames POD-safe (Date @@toPrimitive,
  replace-callback, 3 regexp @@symbol natives, compile). RULE REFINED: the
  crash requires an UNCONSTRUCTED non-POD at the throw; construct all
  non-PODs before the first throwing call (destructors on valid objects are
  safe). tmp/psmell4.js battery.
- Item 5 (e130925f): single [[Delete]] engine; delete_prop forwards; ns
  variants merged as union (+2).
- Item 6 (cd5ec906): FunctionLoweringScope RAII replaces the 10-site
  prologue; ALSO now covers breakTargetStack_/pendingLabel_ (which NO site
  cleared). Net -120 lines.
- Item 7 (8c52a121): finishClosure() shared MakeClosure trailer (7 sites,
  method site keeps emitWithBind=false explicitly). Net -100 lines.
- Item 8 (19406eb7): MEDIUM longjmp hoists (Join/TA-join/fromCodePoint;
  toLocaleString + Intl ctors audited already-safe); g_obj_integrity keys
  now PINNED (scanner) — full-GC stale-key type confusion traded for a
  bounded leak on dropped frozen objects.
- All gated: golden 267/279 no regressions throughout, node 300/300, family
  sweeps 0 lost (+~6 gained incidentally). Tier 3 remains (below).

Successor to the 2026-06-16 audit (ts_call_N unification, TIER 1 done). Evidence
base: three parallel read-only surveys + the 2026-07-11/12 grind session, where
every smell class below caused at least one shipped bug.

## Survey findings (site-verified)

**Runtime divergent duplicates:**
- `[[Delete]]`: 2 full engines — `ts_object_delete_prop` (TsObject.cpp:6907)
  vs `ts_object_delete_property` (:7012). Drift: Proxy routing, accessor-half
  cleanup, `g_array_default_iterator_deleted`, module-namespace handling each
  live in only ONE copy. Two shipped bugs this week.
- "Type(v) is Object": ~20 open-coded brand-exclusion lists (STRG/CONS/SYMB/
  BIGI). Three LIVE BUGS found: `instanceof` omits CONS (Primitives.cpp:836,
  :854 — rope string treated as object), `Iterator.concat` omits BIGI
  (TsGlobals.cpp:4134), TsArray_Methods.cpp:73 omits SYMB/BIGI.
  Canonical 4-tag form exists at TsObject.cpp:5813.
- `typeof`: 2 engines — `ts_typeof` (Primitives.cpp:681, correct) vs
  `ts_value_typeof` (TsObject.cpp:4257, MISSES SYMB at offset 0 → bare
  TsSymbol reports "object").
- Iterator acquisition: 5 independent walks (`ts_iterator_get` misplaced in
  TsPromise.cpp:1618; Iterator.concat's own legacy walk TsGlobals.cpp:4060+;
  `ts_destructure_iterate` TsArray.cpp:3162; inline result-checks accepting
  pointer-shaped primitives; GetIteratorDirect duplicated in TsMap.cpp:1624
  and TsGlobals.cpp:6591).
- Property-GET ladders: accessor (`__getter_`) consultation inconsistent
  across `ts_object_get_property_impl` / `ts_object_get_dynamic` / per-type
  getters; symbol-keyed accessors on TsFunction can be missed (the DDA
  custom-searcher residual).
- CLEAN: ToBoolean / loose-eq / strict-eq are single-definition. Model state.

**Compiler structural duplication:**
- Function-visitor prologue: exactly 10 sites (this week's `withEnvEntered_`
  was a 10-site anchor-patch). THREE latent divergences: site 4
  (`lowerMethodDefinitionToFunction`) misses `innerFuncClosures_` save/restore;
  sites 5-10 (Classes.cpp inline + defaultCtor + decorator-init) never clear
  `loopStack_/switchStack_/labeledLoops_/pendingCaptures_/innerFuncClosures_`;
  three different strictCode_ idioms.
- For-of dual lowering: `visitForOfStatement` (HIR protocol, has
  require_object + IteratorClose) vs `lowerYieldStar`
  (HIRToLLVM_ControlFlow.cpp:1019, full LLVM-level reimpl, NO require_object
  guard, independent close semantics).
- Builtin-name tables: lowerLoadGlobal chain (~80) / ts_global_ctor_by_name
  (39) / builtinObjects set (34). Gaps: `Iterator` missing from lowerLoadGlobal;
  `Proxy`, GeneratorFunction-family missing from ctor-by-name (breaks interp
  `new Proxy`); `Intl`/`BigInt`/entire TypedArray family missing from
  builtinObjects.
- MakeClosure trailers: 7 sites, capture-marking loop copy-pasted ×4, method
  site omits ts_with_bind_fn.
- `ts_destructure_require_object`+`ts_destructure_iterate` open-coded at ≥5
  sites alongside the canonical lowerArrayBindingPattern.

**Crash-class hazards:**
- longjmp-through-non-POD frames (the 0xc0000374 class, bit twice):
  HIGH: ts_date_symbolToPrimitive_native (Builtins:2411, `std::string hint`
  live at throw); ts_string_replace_callback_regex (:324, vector+string live
  across user callback); ts_regexp_symbol_replace/split/matchAll natives
  (:3198/:3322/:3499, many strings live across throwing getters/exec);
  ts_regexp_compile_native (:2897, strings live across Symbol coercion).
  MEDIUM: TsArray::Join (stringstream across per-element ToString),
  ts_array_toLocaleString, Intl ctor bodies ×3, String.fromCodePoint lambda.
  CLUSTER: ~40 sites in TsTemporal.cpp (option-parsing strings live at
  RangeError throws).
- GC rooting: CLEAN — every persistent GC-pointer container has
  scanner+fixup (or documented-correct scanner-only/fixup-only). One edge:
  `g_obj_integrity` weak-key table goes stale on FULL GC (minor-only fixup) —
  address-reuse could inherit a frozen/sealed level.

## Plan

Rules: one build cycle per batch of staged edits; every merge gated
golden-IR + node + targeted test262 families; full sweep per ~5 merges.
Refactor merges must measure ~0/0 on family diffs EXCEPT Tier-1 items, which
fix live bugs and should GAIN.

### Tier 1 — surgical, live-bug-fixing (one session, 4 cycles)
1. `ts_value_is_object(TsValue*)` canonical helper (promote TsObject.cpp:5813
   form); replace all ~20 open-coded lists. Fixes CONS-instanceof +
   BIGI-Iterator.concat. Gates: instanceof/Iterator/Proxy families.
2. `ts_value_typeof` forwards to `ts_typeof`. Fixes symbol-as-"object".
   Gates: typeof + Symbol families.
3. Builtin-table gap patch (NOT full unification): Iterator→lowerLoadGlobal;
   Proxy + GeneratorFunction/AsyncFunction/AsyncGeneratorFunction→
   ctor-by-name; Intl/BigInt/TypedArray family→builtinObjects set.
   Gates: built-ins/Iterator, Proxy, TypedArrayConstructors, eval families.
4. Longjmp HIGH×4: POD-ify (TsString-concat / char-buf pattern per 75b7a80f).
   Probes: Date toPrimitive bogus-hint, throwing replacer, RegExp-subclass
   throwing getters, compile(Symbol()). Gates: Date/String/RegExp families.

### Tier 2 — consolidations (2-3 sessions)
5. `[[Delete]]` unification: port module-ns + array-iterator-flag branches
   into ts_object_delete_property; reduce ts_object_delete_prop to a
   forwarder. Gates: delete family, Reflect, Proxy, module namespaces.
6. `FunctionLoweringScope` RAII struct (12 members: tryDepth_, withDepth_,
   withLexical_, withEnvEntered_, strictCode_, pendingCaptures_,
   innerFuncClosures_, loopStack_, switchStack_, labeledLoops_,
   currentBlock_, currentFunction_; reset policy baked in). Adopt
   site-by-site with golden-IR diff per site. Auto-fixes the 3 latent leaks.
   Gates: full function/class/closure surface.
7. `finishClosure()` trailer extraction (7 MakeClosure sites) — rides with 6.
8. Longjmp MEDIUM×4 (Join, toLocaleString, Intl ctors, fromCodePoint) +
   `g_obj_integrity` full-GC stale-key fix (clear or re-key on full GC).

### Tier 3 — architecture (pick per appetite)
9. Iterator subsystem: new TsIterator.cpp home for ts_iterator_get;
   Iterator.concat/destructure/Array.from/GetIteratorDirect×2 all route
   through it + ts_iterator_step_require_object; shared emitIteratorLoop
   for visitForOfStatement AND lowerYieldStar; destructure open-coding →
   lowerArrayBindingPattern. NOTE: directly unblocks the banked for-of/dstr
   test vein (~30 tests + the `for ([a] of [true])` crash).
10. Property-GET ladder unification: ToPropertyKey once + symbol-key
    canonicalization at ts_object_get_dynamic_checked; per-type getters
    become leaf handlers; dispatch_map_chain_getter the only accessor walk.
    Biggest blast radius — its own sweep.
11. Canonical BUILTIN_GLOBALS table (name, getter, is-ctor, is-namespace)
    generating lowerLoadGlobal / ctor-by-name / builtinObjects.
12. TsTemporal longjmp cluster: mechanical POD-ify anchor-patch (~40 sites).

## Expected side profit
Tier 1 alone should move test262: symbol-typeof, CONS-instanceof,
Iterator/Proxy resolution, and the four HIGH longjmp sites are all
test-covered surfaces. Tier 3 item 9 reopens a banked ~30-test vein.
