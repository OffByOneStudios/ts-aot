// String utilities module
export function greet(name: string): string {
    return "Hello, " + name + "!";
}

export function uppercase(s: string): string {
    // For now, just return the string (proper implementation later)
    return s;
}
