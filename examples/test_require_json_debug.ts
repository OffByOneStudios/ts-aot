// Debug test to see what's in the array
console.log("=== Debug JSON Array ===");

const data = require("./test_data_full.json");

// Manually check the array
console.log("typeof data.tags:", typeof data.tags);
console.log("tags:", data.tags);

console.log("=== Done ===");
